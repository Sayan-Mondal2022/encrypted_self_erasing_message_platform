# Server
PORT=5000

# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/readonce

# Encryption master key (32 bytes, base64 encoded)
# Generate with: openssl rand -base64 32
READONCE_MASTER_KEY='XRHwh5pSC/zFRbTbE/MyhtLgIG0r9saunjU3d25WACE='

# TTL (in seconds) for auto-deleting unread messages (optional)
# Example: 86400 = 24h, 0 = disabled
TTL_SECONDS=86400
